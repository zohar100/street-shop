<!DOCTYPE html>
<html>
    <head>
        <title>Shop</title>
        <script src="https://kit.fontawesome.com/11d17a67dc.js"></script>
        <link href="https://fonts.googleapis.com/css2?family=Bangers&family=Montserrat:wght@300&display=swap" rel="stylesheet">
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href= "/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top mb-2">
            <div class="container">
                <!--Left side-->
                <a class="navbar-brand font" href="#"> Street <i class="far fa-hand-peace"></i> Shop</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                    <a class="nav-link" href="/">Home</span></a>
                    </li>
                    <li class="nav-item <%= typeof page !== 'undefined' && page === 'shop' ? 'active' : '' %>">
                    <a class="nav-link" href="/">Shop</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
                <!--Right side-->
                <ul class="navbar-nav ml-auto">
                    <% if(!currentUser){ %>
                    <li class="nav-item <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>">
                    <a class="nav-link" href="/login">LogIn <i class="fas fa-sign-in-alt"></i></a>
                    </li>
                    <% } else{ %>
                    <li class="nav-item">
                        <div class="dropdown">
                            <button class="bg-dark nav-link dropdown-toggle border-0 <%= typeof page !== 'undefined' && page === 'profile' ? 'active' : '' %>" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <%= currentUser.username %> <i class="fas fa-user"></i>
                            </button>
                            <div class="dropdown-menu bg-dark" aria-labelledby="dropdownMenuButton">
                                <a class="nav-link" href="/users/<%= currentUser._id %>"><i class="fas fa-user"></i> profile</a>
                                <a class="nav-link" href="/cart/<%= currentUser._id%>"><i class="fas fa-shopping-basket"></i> Cart</a>
                                <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                            </div>
                        </div>
                    </li>
                    <% } %>
                </ul>
                </div>
            </div>
          </nav>
          <div class="flash fixed-top mt-5">
            <% if(error && error.length > 0) {%>
            <div id="timeoutMessage" class="alert alert-danger mt-1" style="opacity: 1; transition:3000ms;" role="alert">
                <%= error %>
            </div>
            <%}%>
            <% if(success && success.length > 0) {%>
            <div id="timeoutMessage" class="alert alert-success mt-1" style="opacity: 1; transition:3000ms;" role="alert">
                <%= success %>
            </div>
            <%}%>
        </div>
        <br>
        <br>